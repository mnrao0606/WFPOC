_schema-version: "3.1"
ID: ManageSupplierInvoices
description: A simple CAP project.
version: 1.0.0
modules:
- name: ManageSupplierInvoices-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: ManageSupplierInvoices-db
  - name: ManageSupplierInvoices-auth
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
  build-parameters:
    builder: npm-ci
- name: ManageSupplierInvoices-db-deployer
  type: hdb
  path: db
  requires:
  - name: ManageSupplierInvoices-db
  parameters:
    buildpack: nodejs_buildpack
- name: ManageSupplierInvoices
  type: approuter.nodejs
  path: app/
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: ManageSupplierInvoices-auth
  - name: ManageSupplierInvoices-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: ManageSupplierInvoices-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: ManageSupplierInvoices-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - companycodefiori.zip
      name: companycodefiori
      target-path: resources/
    - artifacts:
      - invoicefiori.zip
      name: invoicefiori
      target-path: resources/
- name: companycodefiori
  type: html5
  path: app/companycodefiori
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: invoicefiori
  type: html5
  path: app/invoicefiori
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: ManageSupplierInvoices-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: ManageSupplierInvoices-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: ManageSupplierInvoices-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: ManageSupplierInvoices-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: ManageSupplierInvoices-html5-srv
    service-plan: app-host
- name: ManageSupplierInvoices-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: ManageSupplierInvoices-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npx -p @sap/cds-dk cds build --production
